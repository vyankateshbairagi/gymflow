import { Badge } from "@/components/ui/badge";

export function MockDataBadge() {
  return (
    <Badge variant="warning" className="font-normal">
      Sample data — not yet connected to the database
    </Badge>
  );
}
