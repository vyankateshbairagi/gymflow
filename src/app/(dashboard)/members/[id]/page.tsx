import { notFound } from "next/navigation";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { db } from "@/lib/db";
import { getCurrentUser } from "@/lib/auth";
import { can } from "@/lib/permissions";
import { redirect } from "next/navigation";

export default async function MemberDetailsPage({ params }: { params: Promise<{ id: string }> }) {
  const user = await getCurrentUser();
  if (!can(user.role, "members:view")) redirect("/dashboard");
  const { id } = await params;
  const member = await db.member.findFirst({ where: { id, organizationId: user.organizationId }, include: { subscriptions: { orderBy: { createdAt: "desc" }, take: 5, select: { id: true, startDate: true, endDate: true, amount: true, status: true, plan: { select: { name: true } } } }, payments: { orderBy: { paymentDate: "desc" }, take: 5, select: { id: true, amount: true, paymentDate: true, status: true, subscription: { select: { plan: { select: { name: true } } } } } } } });
  if (!member) notFound();
  const currentMembership = member.subscriptions.find((subscription) => subscription.status === "ACTIVE");
  const totalFees = member.subscriptions.reduce((sum, subscription) => sum + Number(subscription.amount), 0);
  const totalPaid = member.payments.filter((payment) => payment.status === "COMPLETED").reduce((sum, payment) => sum + Number(payment.amount), 0);
  const totalRefunded = member.payments.filter((payment) => payment.status === "REFUNDED" || payment.status === "PARTIALLY_REFUNDED").reduce((sum, payment) => sum + Number(payment.amount), 0);
  return <div className="space-y-6"><div><p className="text-sm text-muted-foreground">Member profile</p><div className="flex items-center gap-3"><h1 className="text-2xl font-semibold tracking-tight">{member.name}</h1><Badge variant={member.status === "ACTIVE" ? "success" : "secondary"}>{member.status}</Badge></div></div><Card><CardHeader><CardTitle>Contact and profile</CardTitle></CardHeader><CardContent className="grid gap-5 sm:grid-cols-2"><div><p className="text-xs text-muted-foreground">Phone</p><p>{member.phone}</p></div><div><p className="text-xs text-muted-foreground">Email</p><p>{member.email ?? "-"}</p></div><div><p className="text-xs text-muted-foreground">Date of birth</p><p>{member.dateOfBirth?.toLocaleDateString() ?? "-"}</p></div><div><p className="text-xs text-muted-foreground">Gender</p><p>{member.gender ?? "-"}</p></div><div><p className="text-xs text-muted-foreground">Address</p><p>{member.address ?? "-"}</p></div><div><p className="text-xs text-muted-foreground">Emergency contact</p><p>{member.emergencyContact ?? "-"}</p></div><div><p className="text-xs text-muted-foreground">Join date</p><p>{member.joiningDate.toLocaleDateString()}</p></div></CardContent></Card><Card><CardHeader><CardTitle>Financial Summary</CardTitle></CardHeader><CardContent className="grid gap-4 sm:grid-cols-3"><div><p className="text-xs text-muted-foreground">Total fees</p><p className="font-semibold">₹{totalFees.toFixed(2)}</p></div><div><p className="text-xs text-muted-foreground">Total paid</p><p className="font-semibold">₹{totalPaid.toFixed(2)}</p></div><div><p className="text-xs text-muted-foreground">Total refunded</p><p className="font-semibold">₹{totalRefunded.toFixed(2)}</p></div></CardContent></Card><div className="grid gap-4 md:grid-cols-3"><Card><CardHeader><CardTitle>Membership</CardTitle></CardHeader><CardContent>{currentMembership ? <div className="space-y-1 text-sm"><p className="font-medium">{currentMembership.plan.name}</p><p>₹{currentMembership.amount.toString()}</p><p className="text-muted-foreground">{currentMembership.startDate.toLocaleDateString()} to {currentMembership.endDate.toLocaleDateString()}</p><Badge variant="success">{currentMembership.status}</Badge></div> : <p className="text-sm text-muted-foreground">No active membership yet.</p>}</CardContent></Card><Card><CardHeader><CardTitle>Payment History</CardTitle></CardHeader><CardContent className="space-y-2 text-sm">{member.payments.length === 0 ? <p className="text-muted-foreground">No payments recorded.</p> : member.payments.map((payment) => <div key={payment.id} className="flex justify-between border-b pb-2"><span>{payment.subscription?.plan.name ?? "Payment"}</span><span>₹{payment.amount.toString()}</span></div>)}</CardContent></Card>{[["Payments", "Payment history will appear here."], ["Attendance", "Attendance history will appear here."]].map(([title, text]) => <Card key={title}><CardHeader><CardTitle>{title}</CardTitle></CardHeader><CardContent className="text-sm text-muted-foreground">{text}</CardContent></Card>)}</div></div>;
}